//fizz buzz game

for(let numbe = 1 ; numbe <= 16 ; numbe++) {
  let result = "";

  if(numbe % 3 == 0) {
    result = result + "fizz";
  }

  if(numbe % 5 == 0) {
    result = result + "buzz";
  }

  if(result == "") {
    result = numbe;
  }

  console.log(result);
}
